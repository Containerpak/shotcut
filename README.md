# Shotcut (cpak)

## Installation

```bash
cpak install github.com/containerpak/shotcut
```

## Usage

Start it from its icon in the application menu or by running:

```bash
cpak run github.com/containerpak/shotcut shotcut
```
